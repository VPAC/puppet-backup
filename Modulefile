name 'VPAC-puppet-backup'
version '1.0.1'
summary 'backup a file system with tar and send it to a remote server via SSH'
description 'Backs up filesystems and ssh copies them to remote servers.'
project_page 'https://github.com/VPAC/puppet-backup'

